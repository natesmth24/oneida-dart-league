const standingsBody=document.getElementById('standings-body');leagueData.standings.forEach((t,i)=>{standingsBody.insertAdjacentHTML('beforeend',`<tr><td>${i+1}</td><td><strong>${t.team}</strong></td><td>${t.wins}</td><td>${t.losses}</td><td>${t.gamesWon}</td></tr>`)});const leaders=document.getElementById('weekly-leaders');Object.entries(leagueData.weeklyLeaders).forEach(([category,rows])=>{const body=rows.length?rows.map(r=>`<div class="leader-row"><b>${r.place}</b><span>${r.name}</span><strong>${r.score}</strong></div>`).join(''):'<div class="leader-row"><span>—</span><span>Season not started</span><strong>—</strong></div>';leaders.insertAdjacentHTML('beforeend',`<article class="leader-card"><h3>${category}</h3>${body}</article>`)});const matchups=document.getElementById('matchups');leagueData.upcoming.forEach(m=>{matchups.insertAdjacentHTML('beforeend',`<article class="match-card"><span>Home</span><strong>${m.home}</strong><div class="vs">VS</div><strong>${m.away}</strong><span>Away</span></article>`)});